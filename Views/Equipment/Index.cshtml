@using System.Web.Optimization
@using X.PagedList
@using X.PagedList.Mvc
@using X.PagedList.Mvc.Core
@using X.PagedList.Mvc.Common
@model SearchEquipmentViewModel
@{
    ViewData["Title"] = "Index";
    Layout = "_ManageLayout";
}


    @await Html.PartialAsync("_IndexBodyPartial", Model,
                                 new ViewDataDictionary(ViewData)
                                 {})
    
@section script {
    @{ await Html.RenderPartialAsync("_ValidationScriptsPartial"); }
    <script src="~/lib/bootstrap-table/dist/bootstrap-table.min.js" async></script>
    <script src="~/lib/bootstrap-table/dist/locale/bootstrap-table-zh-CN.min.js" async></script>
    <script>
        function deleteItem(form) {
            $(form).closest("tr").find(".isDeleted").text("Yes");
        }
        function onSearchEquipmentSuccess(){
            $('[data-toggle="table"]').bootstrapTable();
        }
        function onSearchEquipmentFailure(){
            $('[data-toggle="table"]').bootstrapTable();
        }
    </script>
}